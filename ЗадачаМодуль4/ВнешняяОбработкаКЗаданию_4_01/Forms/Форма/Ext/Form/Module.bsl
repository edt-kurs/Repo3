
&НаСервере
Процедура ПосчитатьНаСервере(Ф)
	Переменная3 = Переменная1 + Переменная2;
 	Переменная4 = Переменная1 - Переменная2
КонецПроцедуры

&НаКлиенте
Процедура Посчитать(Команда)
	ПосчитатьНаСервере(1);
КонецПроцедуры
